#include <stdio.h>

int main()
{

    int numero1, numero2, numero3, numero4, numero5, numero6, numero7, numero8, numero9, multiplicador, resultado;
   
    multiplicador = 1;
    numero1 = 1;
    numero2 = 2;
    numero3 = 3;
    numero4 = 4;
    numero5 = 5;
    numero6 = 6;
    numero7 = 7;
    numero8 = 8;
    numero9 = 9;
   
    while(multiplicador <= 9 ) {
        resultado = numero1 * multiplicador;
        printf("%d X %d = %d\n\n", numero1, multiplicador, resultado);
        multiplicador ++;
    }
   
    multiplicador = 1;
    while(multiplicador <= 9 ) {
        resultado = numero2 * multiplicador;
        printf("%d X %d = %d\n\n", numero2, multiplicador, resultado);
        multiplicador ++;
    }
   
    multiplicador = 1;
        while(multiplicador <= 9 ) {
        resultado = numero3 * multiplicador;
        printf("%d X %d = %d\n\n", numero3, multiplicador, resultado);
        multiplicador ++;
    }
   
    multiplicador = 1;
        while(multiplicador <= 9 ) {
        resultado = numero4 * multiplicador;
        printf("%d X %d = %d\n\n", numero4, multiplicador, resultado);
        multiplicador ++;
    }
   
    multiplicador = 1;
        while(multiplicador <= 9 ) {
        resultado = numero5 * multiplicador;
        printf("%d X %d = %d\n\n", numero5, multiplicador, resultado);
        multiplicador ++;
    }
   
    multiplicador = 1;
        while(multiplicador <= 9 ) {
        resultado = numero6 * multiplicador;
        printf("%d X %d = %d\n\n", numero6, multiplicador, resultado);
        multiplicador ++;
    }
   
    multiplicador = 1;
        while(multiplicador <= 9 ) {
        resultado = numero7 * multiplicador;
        printf("%d X %d = %d\n\n", numero7, multiplicador, resultado);
        multiplicador ++;
    }
   
    multiplicador = 1;
        while(multiplicador <= 9 ) {
        resultado = numero8 * multiplicador;
        printf("%d X %d = %d\n\n", numero8, multiplicador, resultado);
        multiplicador ++;
    }
   
    multiplicador = 1;
        while(multiplicador <= 9 ) {
        resultado = numero9 * multiplicador;
        printf("%d X %d = %d\n\n", numero9, multiplicador, resultado);
        multiplicador ++;
    }

    return 0;
}